# API base URL used by the browser.
# In development requests go through the Vite proxy (/api -> http://localhost:5000),
# so this is only needed when calling the backend directly.
# VITE_API_URL=http://localhost:5000